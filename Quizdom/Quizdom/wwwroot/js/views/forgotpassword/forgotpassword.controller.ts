namespace Quizdom.Views.ForgotPassword {
    export class ForgotPasswordController {
        public formData: Models.ForgotPasswordModel = new Models.ForgotPasswordModel();

        static $inject = [];

        constructor() {
          
        }

        public requestPassword() {
            // TODO: Complete process
            console.log('testing request password');
        }
    }
}